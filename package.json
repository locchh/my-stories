{
  "name": "my-stories",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && bun scripts/generate-pages.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "dompurify": "^3.2.6",
    "marked": "^16.1.2"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.2"
  }
}
